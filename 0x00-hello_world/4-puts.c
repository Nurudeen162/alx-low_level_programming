#include <stdio.h>
/**
 * main - a program that prints followed by new line, use function puts
 * Return: 0 (success)
 */
Int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
